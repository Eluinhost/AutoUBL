name: ${project.name}
main: ${project.groupId}.${project.artifactId}.Entry
version: ${project.version}
description: Automatically ban player's on the UBL
author: ghowden/Eluinhost
database: false
commands:
  ublrefresh:
    permission: uhc.ubl.refresh
permissions:
  uhc.ubl.refresh:
    default: op